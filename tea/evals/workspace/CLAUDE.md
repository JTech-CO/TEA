# Project notes

For any multi-step coding task in this repo — edits across files, debugging,
refactoring, reviewing, or exploring the codebase — invoke the `tea` skill
first, before reading any file. Skip it for non-coding requests, for trivial
single-file edits, and for requests to shorten or compress code.

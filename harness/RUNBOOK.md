# RUNBOOK.md — 런북 (증상 → 원인 → 조치)

> 처음 겪는 이슈는 임시 처치 후 `PROGRESS.md`에 기록하고, 반복되면 여기 행으로 승격한다. 불변식 관련 증상은 조치에 INV-n를 병기한다.

## 일반
| # | 증상 | 흔한 원인 | 조치 |
|---|---|---|---|
| 1 | 환경/의존성 설치 실패 | 버전 불일치, 락파일 깨짐 | `docs/ENVIRONMENT`대로 재설치, 버전 고정 |
| 2 | 스크립트 실행 실패 | 경로·런타임 버전 | 에러 위치부터 좁혀 수정 |
| 3 | 환경별 동작 차이(로컬↔CI) | 환경변수·버전·권한 차이 | 환경 동등성 확보, 버전 일치 점검 |

## TEA 고유
| # | 증상 | 흔한 원인 | 조치 |
|---|---|---|---|
| 4 | 토큰 수가 측정할 때마다 다름 | 토크나이저가 고정되지 않음 | 하나로 고정하고 `docs/ENVIRONMENT`에 명시(INV-3). 절대값보다 일관성 |
| 5 | 스킬이 코딩 작업에서 발화하지 않음 | 정의역 열거 부족, 또는 **평가 문항이 너무 쉬움** | 문항 난이도 먼저 점검. 에이전트가 자력 처리 가능한 문항은 문안과 무관하게 미발화 |
| 6 | 비코딩 작업에서 오발화 | 키워드가 넓음 | "tea" 키워드 우선 제거(`docs/DESIGN` §3.2 미확정 항목) |
| 7 | 골프 요청에서 발화 | 비적용 문구 약함 | description 3문단의 골프 배제 문구 강화. M1 DoD 4는 0건 기준 |
| 8 | 핸들 추출 결과가 전부 0 | 파서가 실제 로그 포맷을 못 읽음 | 합성 픽스처 통과는 무의미. 실제 로그 1건으로 포맷 재확인(M4 주의) |
| 9 | 완료 통지 데이터 유실 | 통지 시점에만 확보 가능한데 배치 처리함 | 통지마다 즉시 저장. 유실된 실행은 폐기하고 재실행 |
| 10 | B군이 선행 공개치와 크게 다름 | 모델·버전·Ponytail 모드 차이, 태스크셋 변경 | **C군 측정 금지.** 원인 규명 먼저(M5 DoD 1). 환경 문제와 TEA 문제 분리 |
| 11 | C군이 B군보다 나쁨 | 계측 오류 / 특정 규칙 / 가설 반증 | `HARNESS.md` §2.3 순서 — 계측 의심 → 규칙별 분리 → 반증 보고. **규칙 조정으로 수치 맞추기는 레드라인** |
| 12 | 앵커 불일치(W2 핸들) 증가 | W2를 과하게 좁게 적용 | W2 완화 후 재측정. 이 핸들은 과긴축 조기 경보다(M6 DoD 4) |
| 13 | 델타가 표준편차보다 작음 | 표본 부족, 태스크에 계층 C 여지 없음 | 반복 증가. 그래도 작으면 "결론 없음"으로 기록. 태스크별 분해 병행 |
| 14 | 모델 A는 개선, 모델 B는 악화 | 사다리 추론 비용 편차 | 한 모델 통과로 채택 금지(`docs/DESIGN` §7.3). 역전 조건 문서화 |
| 15 | 예산(INV-3) 초과 | 규칙 또는 근거가 늘어남 | **규칙 수를 줄이지 근거를 줄이지 않는다**(M2 리스크). 근거 축소는 준수율을 떨어뜨림 |
| 16 | 금칙어 검출됨(INV-5) | 규칙을 결과 수준으로 서술 | 행동 수준으로 재작성. "읽기를 줄인다" → "열기 전에 검색으로 특정한다" |
| 17 | 규칙 수와 핸들 수 불일치(INV-6) | 측정 핸들 없이 규칙 추가 | 핸들을 정의하거나 규칙을 제거. 예외 없음 |
| 18 | 플러그인 전환 후 성능 악화 | 매 턴 주입이 트리거당 로드보다 비쌈 | M7 DoD 8로 검출. 강도별 텍스트 분기 축소 또는 스킬 상태 유지 |
| 19 | 탐색 서브에이전트에 WRITE 축이 주입됨 | 스코프 미분리 | 축 선별 주입 확인(`docs/DESIGN` §8.3). 편집 권한 없는 에이전트에 편집 규칙은 예산 낭비 |
| 20 | 발화율이 0에 가깝고 오류 트라이얼이 다수 | **턴 상한이 낮아 발화 세션만 선택적으로 탈락** | 스킬 호출은 턴을 1회 더 쓴다 → 상한 초과 → "오류"로 제외 → 미발화 세션만 집계에 남는다. 상한을 올리고, **오류 트라이얼의 발화 여부를 먼저 확인**하라(M1 v1 실사례: 오류 26건 중 18건이 발화) |
| 21 | 평가 세션이 예상보다 많은 턴을 씀 | 도구 화이트리스트가 권한 거부를 유발 | `--allowedTools`는 도구 목록을 제한하지 못하고 거부만 만든다. 거부된 호출이 턴을 소진한다. 일회용 격리 워크스페이스에서는 `--permission-mode bypassPermissions`를 쓴다 |
| 22 | 평가 결과에 절단(truncated) 세션이 섞임 | 상한 도달을 "미발화"로 집계 | 절단된 미발화는 **판정 보류**다(늦게 발화했을 수 있음). 발화는 절단과 무관하게 유효 관측. 3상태로 분류하고 보류분은 게이트를 막게 하라 |
| 23 | 실행 도중 주간 사용량 한도 도달 | 장시간 배치 실행이 한도를 소진 | 러너가 한도 오류를 인증 실패처럼 즉시 전체 중단(fail-fast). 부분 데이터로 판정 금지 — 완전성 게이트가 차단한다. 리셋 후 전량 재실행 |

## 행 추가 규칙
같은 이슈를 2회 이상 겪으면 행으로 승격. 형식: 증상(관측 가능) / 가장 흔한 원인 순 / 최소 조치. 불변식 관련이면 INV-n 병기.

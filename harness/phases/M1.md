# M1 — description 확정 ★

> 근거: docs/DESIGN §3(트리거 명세)·§5(평가셋)·§11(2번이 1번에 선행). 발화하지 않는 본문은 측정할 수 없다 — M2 선행 조건.

## 목적
트리거 발화가 게이트 수치로 보장된 description 확정본을 얻는다.

## DoR (진입조건)
- [x] M0 DoD 통과 (2026-08-14, phases/M0.md 증거)
- [x] 발화 판정 프로토콜 정의 — docs/ENVIRONMENT "트리거 발화 판정 프로토콜" 절에 고정 (2026-08-14)

## 할 일
1. description v1 — DESIGN §3.2 초안 기준, 3문단 구조(정의역/recall/precision) 유지
2. tea/evals/trigger.json — 20문항, §5.1 구성(양성 12/음성 8, 유형별 분포 준수)
3. 60/40 학습·검증 분할 명시 — 문안 개선은 학습셋으로, 채택 판단은 검증셋 점수로
4. 문항당 3회 실행, 발화율 집계(recall/precision)
5. 미달 시: recall→정의역 열거 확장, precision→비적용 문구 강화, "tea" 키워드 제거 검토(§3.2 미확정)

> **측정 조건 개정(ADR-0010, 2026-08-18)**: DoD 2~4의 측정 대상은 "스킬 + CLAUDE.md 로딩 어댑터"다. 게이트 수치는 원안 그대로.

## DoD (완료 게이트)
| # | 게이트 | 판정 방법 | 불변식 |
|---|---|---|---|
| 1 | trigger.json 구성 준수 | 양성 12/음성 10, §5.1 유형 분포(골프는 감사 반영으로 2→4 강화) | — |
| 2 | 검증셋 recall ≥ 5/6 | 문항당 3회 발화율. 유형당 1문항이라 "유형 5종 전부 ≥1회 AND 총 미발화 ≤2"와 동치 | — |
| 3 | 검증셋 precision ≥ 7/8 | 〃 (골프 제외 분모 — DoD 4와 이중 계상 방지) | — |
| 4 | 골프·압축 4문항 발화 0건 | 〃 (RUNBOOK #7) | — |
| 5 | 증거: 문항×회차 발화 매트릭스 | 본 파일 증거 칸 + PROGRESS | — |
| 6 | 무결성: 판정 불가 0 · 폐기된 발화 관측 0 · 스킬 미노출 0 | 러너 게이트 출력 | — |

## 검증 명령
```
node scripts/trigger-eval.js --smoke             # 계측 배선 검증 (실측정 전 필수 — RUNBOOK #8 원리)
node scripts/trigger-eval.js --set all --gate    # 전 문항 × 3회 + DoD 2~4 게이트 판정
node scripts/trigger-eval.js --set train         # 학습셋만 (문안 반복 개선용 — 채택 판단 금지)
```

## 증거
| 게이트 | 명령 | 핵심 출력 | 일시 |
|---|---|---|---|
| 계측 배선 | `node scripts/trigger-eval.js --smoke` | PASS — S-POS 발화(턴1~2)·S-NEG 미발화 | 2026-08-15 |
| DoD 2 예비(학습) | v2·v3 학습셋 실측 (문항당 3회) | v2: 13/19(68%) · v3: 9/20(45%) — navigation 0/6 전멸. 게이트(≈87%+유형 전수) 미달 → **STOP** | 2026-08-18 |
| DoD 3 예비(학습) | v3 학습셋 | 음성 미발화 18/18 (비코딩·단발 전부) | 2026-08-18 |
| DoD 4 예비(학습) | v3 학습셋 | 골프 N7·N9 발화 0/6 | 2026-08-18 |
| 무결성 | 러너 게이트 출력 | 폐기 발화 0 · 스킬 미노출 0 · 권한거부 0 | 2026-08-18 |

> 결과 원본: `tea/evals/results/run-2026-08-17T*.json` (증분 저장, runKey·init 지문 포함). 검증셋은 미실행 — 학습셋 미달 상태에서 검증셋 소모는 판단 오염이라 보류. STOP 상세와 선택지는 PROGRESS.md 막힘 기록.

**M1 완료 판정 (2026-08-18) — DoD 전부 통과.** 최종 시스템: description **v1**(DESIGN §3.2 초안 원안) + CLAUDE.md 로딩 어댑터 2문장(ADR-0010). 전체 22문항 × 3회 = 66트라이얼 전량 유효(커버리지 결손 0, 재추첨 0, 미노출 0):

| 게이트 | 결과 |
|---|---|
| DoD 2 recall(검증) | **15/15** (기준 ≥13) — 유형 5종 전부 3/3 |
| DoD 3 precision(검증) | **6/6** (기준 ≥6) |
| DoD 4 골프 발화 | **0/12** |
| 무결성 4종 | 전부 PASS |

발화는 전 트라이얼 턴 2에서 발생 — 어댑터에 의한 준결정적 로딩. 재현: `tools/node22/node.exe scripts/trigger-eval.js --score "<runKey 4d… 파일들>" --gate --gate-set all`. 결과 원본: `tea/evals/results/run-2026-08-18T*.json`.

**교훈 기록**: description 문안 수정(v2·v3)은 유기적 발화율을 45~68% 밖으로 끌어내지 못했고(반증 확정), 어댑터 도입 후 원안(v1) 문안으로 복원해도 만점이 나왔다 — 부족했던 것은 문구가 아니라 로딩 경로였다.

**선택지 A(픽스처 확장) 결과 (2026-08-18).** 31파일·804줄로 확장(문항·시드 버그 불변): recall 52%(11/21), navigation 첫 발화(1/3), 그러나 골프 N7 1/3 발화로 DoD 4 훼손. organic 양성 풀링 발화율 ≈47% — 검증 게이트(13/15) 통과 확률 ≈0.4%. **description 단독 트리거 반증 확정** — TECHNICAL §5.3.1의 전제가 이 호스트·모델에서 성립하지 않음. 재현: `tools/node22/node.exe scripts/trigger-eval.js --score "tea/evals/results/run-2026-08-17T22-37-56-train.json,tea/evals/results/run-2026-08-17T22-53-25-train.json" --gate-set train`

## 롤백
description은 문안이므로 이전 버전 복원으로 끝. 평가셋 문항 수정은 새 버전으로(판정에 쓴 문항은 불변 유지).

## 리스크
- 평가 문항이 너무 쉬움 → 자력 처리 가능 문항은 문안과 무관하게 미발화(RUNBOOK #5). 난이도 먼저 점검
- "tea" 일반명사 오발화 → precision 미달 시 키워드 제거(DESIGN §3.2)
- 실행 환경 편차 → 동일 클라이언트·동일 모델 고정, docs/ENVIRONMENT에 기록

# M0 — 기반/스캐폴딩

> 근거: harness/PROGRESS.md 다음 할 일, INVARIANTS.md 검증 방법 열. 아키타입 1 — 빌드·타입체크 대신 계측 스크립트와 예산 게이트가 스캐폴딩이다(gates/DOD_GUIDE.md §3).

## 목적
이후 모든 phase의 게이트(INV-1·2·3·5·6)를 명령 한 줄로 판정할 수 있는 상태를 만든다.

## DoR (진입조건)
- [x] 백서 2종 존재 (docs/TECHNICAL·DESIGN)
- [x] 하네스 팩 존재 — 단, 팩이 참조하는 phases/·decisions/·gates/·docs 보조 문서가 부재였음. 본 phase에서 결손 보완
- [x] Node ≥ 18 (실측 v25.2.0), npm 11.6.2, git 2.54.0

## 할 일
1. 하네스 팩 결손 보완 — phases/(_TEMPLATE, M0~M8), decisions/(ADR-0001~0009), DOD_GUIDE → gates/ 이동
2. git 초기화, .gitignore(INV-2), .gitattributes(LF 고정), pre-commit 시크릿 스캔(INV-1), 루트 CLAUDE.md 포인터
3. docs/ENVIRONMENT — 런타임·토크나이저 고정(ADR-0009)·검증 명령
4. docs/FILE_TREE — 구조와 경계 규칙 / docs/README — 백서 세트 정의
5. rules/catalog.json — 규칙 원장 (사다리 L1~4 + R·W·T 측정 핸들 12종 + X1~4 + P1)
6. 검증 스크립트 — token-budget(INV-3)·forbidden-terms(INV-5)·rule-audit(INV-6)·secret-scan(INV-1·2)
7. selftest — 픽스처로 통과·실패 양방향 판정을 자기시험

## DoD (완료 게이트)
| # | 게이트 | 판정 방법 | 불변식 |
|---|---|---|---|
| 1 | 검증 스크립트 3종이 픽스처에서 통과·실패를 정확히 판정 | `npm run selftest` 전항 PASS | INV-3·5·6의 수단 |
| 2 | 시크릿 스캔이 키 패턴을 차단 (staged 실전 포함) | selftest 케이스 + 실제 커밋 차단 시험 | INV-1의 수단 |
| 3 | pre-commit 훅 활성 | `git config core.hooksPath` = `.githooks` | INV-1 |
| 4 | .gitignore가 .env·세션 로그·벤치 아티팩트 커버 | 파일 내용 확인 | INV-2 |
| 5 | 토크나이저 고정 문서화 | docs/ENVIRONMENT + ADR-0009 존재 | INV-3 전제 |
| 6 | 규칙 원장 정합 — 측정 축 규칙 12개 전부에 핸들 | `npm run check:rules` PASS | INV-6 |
| 7 | 경계 규칙 문서화 | docs/FILE_TREE 존재 + 경계 6항 | — |

## 검증 명령
```
npm run selftest
npm run check:rules
git config core.hooksPath
```

## 증거
| 게이트 | 명령 | 핵심 출력 | 일시 |
|---|---|---|---|
| 1 자기시험 | `npm run selftest` | 전항 통과 10/10 (통과·실패 양방향 판정 검증) | 2026-08-14 |
| 2 시크릿 차단(실전) | AKIA 픽스처 staged 후 `git commit` | `[secret-scan] 커밋 차단: AWS 액세스 키 패턴 검출 (INV-1)`, exit 1 | 2026-08-14 |
| 3 훅 활성 | `git config core.hooksPath` | `.githooks` | 2026-08-14 |
| 4 .gitignore | 파일 검사 | `.env`·`bench/artifacts`·`logs/`·`*.session.jsonl` 등 커버 | 2026-08-14 |
| 5 토크나이저 | docs/ENVIRONMENT + ADR-0009 | gpt-tokenizer 3.4.0, cl100k_base 고정 | 2026-08-14 |
| 6 원장 정합 | `npm run check:rules` | 규칙 21개 / 측정 축 12개 / 핸들 12개, PASS | 2026-08-14 |
| 7 경계 문서화 | docs/FILE_TREE.md | 트리 + 경계 규칙 6항 | 2026-08-14 |

**판정: DoD 7/7 통과 — M0 완료 (2026-08-14)**

## 롤백
스크립트·문서는 독립 파일 — 해당 파일 삭제로 복원. git 초기화 롤백은 .git/ 삭제(이력 소실 주의).

## 리스크
- 토크나이저 절대값이 실제 과금 토큰과 다름 → 일관성 우선 원칙(ADR-0009, RUNBOOK #4). 예산은 cl100k_base 기준으로 해석
- npm 공급망 변동 → package-lock.json 커밋으로 버전 고정
- 픽스처 통과가 실전 통과를 보장하지 않음 → 픽스처는 판정 로직 검증용. 실전 판정은 M2에서 실제 산출물로(RUNBOOK #8과 동일 원리)

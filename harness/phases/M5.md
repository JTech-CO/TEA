# M5 — 3군 벤치마크 ★

> 근거: docs/TECHNICAL §8.2·8.3, docs/DESIGN §6. 아키타입 5·6 — 정합성 대상은 선행 공개치와 B군 재현치.

## 목적
채택 판정(M6)의 입력이 되는 3군 × 12태스크 × n=4 × 모델 2종 데이터셋.

## DoR (진입조건)
- [ ] M4 DoD 통과
- [ ] **검증 모델 2종 확정 (외부 입력 — 미확정 시 진입 불가)**
- [ ] 실행 예산·시간 확보 (12태스크 × 3군 × n=4 × 2모델 = 288 실행)

## 할 일
1. B군 선행 재현 — 공개치와 대조, 불일치 시 원인 규명 먼저 (RUNBOOK #10 — 규명 전 C군 측정 금지)
2. 본 실행 — 3군 전체, 완료 통지 즉시 저장
3. 집계 — mean±sd, C−B 델타, 부지표(재시도율·도구 호출 실패율), 핸들 12종
4. 적대적 티어 실행 (INV-4 판정 입력 — 판정 자체는 M6)

## DoD (완료 게이트)
| # | 게이트 | 판정 방법 | 불변식 |
|---|---|---|---|
| 1 | B군 재현성 판단 기록 (공개치 대비) | 대조표 | — |
| 2 | 실행 완전성 — 결측 없음 (유실분 재실행) | 데이터 감사 | INV-9 |
| 3 | 총 토큰에 reasoning 포함 집계 | 집계 산출물 | INV-10 |
| 4 | mean±sd 산출 + 반복 횟수 명기 | 〃 | — |
| 5 | 원본 로그 비커밋 (요약 통계만 커밋) | git status | INV-2 |

## 검증 명령
```
(M4에서 확정한 러너·집계 명령)
```

## 증거
| 게이트 | 명령 | 핵심 출력 | 일시 |
|---|---|---|---|
| — | — | — | — |

## 롤백
데이터 수집 phase — 롤백 개념 없음. 오염 실행은 폐기·재실행으로 대응.

## 리스크
- B군 재현 실패 → 환경 문제 우선 규명(RUNBOOK #10). C군 진행 금지
- 분산 > 델타 → 반복 증가, 그래도 작으면 "결론 없음" 기록(RUNBOOK #13)
- **여기서 채택 판정하지 않는다** — 판정은 M6. 중간 수치를 보고 규칙을 손대는 것은 게이트 위장 경로(HARNESS §2.3)

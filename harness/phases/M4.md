# M4 — 계측 하네스 ★

> 근거: docs/TECHNICAL §8(측정 방법론), docs/DESIGN §6(벤치마크 절차). 아키타입 9. M5(측정)의 선행 조건 — 계측 없는 측정은 없다.

## 목적
3군 벤치마크를 실행·수집·집계할 수 있는 상태. 파서가 측정 핸들 12종을 **실제 로그**에서 추출한다.

## DoR (진입조건)
- [x] M2 DoD 통과 (2026-08-18. M3도 완결 — DoD 5/5)
- [x] 대상 레포 접근 — bench/target/에 클론 (2026-08-19)
- [x] Ponytail 4.9.0 설치·모드 확인 (2026-08-19) — env `PONYTAIL_DEFAULT_MODE`가 config를 이김(프로브 실증). 전역 off + 군별 env 주입 전략 확정

## 할 일
1. 세션 로그 파서 — 측정 핸들 12종 추출. 핸들 정의의 원장은 rules/catalog.json
2. 3군 러너 — A(스킬 없음)/B(Ponytail)/C(Ponytail+TEA), headless, 태스크 12티켓 × n=4
3. 집계기 — 총 토큰(input+output+**reasoning**), 턴 수, 재시도율, 도구 호출 실패율; mean±sd·델타
4. 완료 통지 즉시 저장 (RUNBOOK #9 — 배치 처리 금지)
5. 태스크 12 feature 티켓 포팅 + assertion 채점 기준
6. B군 파일럿 1회 — 실제 로그로 파서 검증 (RUNBOOK #8 — 합성 픽스처 통과는 무의미)

## DoD (완료 게이트)
| # | 게이트 | 판정 방법 | 불변식 |
|---|---|---|---|
| 1 | 파서가 실제 로그 1건에서 12핸들 전부 추출 | 파일럿 로그 대상 실행 | INV-6 연계 |
| 2 | 집계 스키마에 reasoning 필드 존재 | 스키마 검사 | INV-10 |
| 3 | 집계 스키마에 LOC 필드 부재 | 〃 | INV-8 |
| 4 | 3군 실행 매트릭스 확인 | 러너 dry-run | INV-9 |
| 5 | B군 파일럿 1회 완주 + 데이터 즉시 저장 | 실행 후 산출물 존재 | INV-2(원본 비커밋) |

## 검증 명령
```
(M4 착수 시 러너 인터페이스와 함께 확정)
```

## 증거
| 게이트 | 명령 | 핵심 출력 | 일시 |
|---|---|---|---|
| DoD 1 파서 12핸들 | `node bench/parse-handles.js <파일럿 로그>` | **실제 로그에서 12/12 추출** (검색 7·읽기 1·편집 1의 실전 세션. 조작적 정의는 파서 주석에 원장 문구와 병기) | 2026-08-19 |
| DoD 2 reasoning 필드 | `node bench/aggregate.js --schema-check` | INV-10 PASS — 파일럿에서 reasoningTokens 685 실측 | 2026-08-19 |
| DoD 3 LOC 부재 | 〃 | INV-8 PASS — 전체 스키마 키 재귀 검사 0건 | 2026-08-19 |
| DoD 4 3군 매트릭스 | `bench/run3.js --dry` | 1태스크 × A,B,C × sonnet,opus × n4 = 24트라이얼 열거 (INV-9) | 2026-08-19 |
| DoD 5 B군 파일럿 | `bench/run3.js --tasks tmpl-be-count --arms B --models sonnet --n 1` | **ok · 총 324,437tok · 10턴 · assertion PASS** · 증분 즉시 저장(bench/results). ponytail 스킬 6종 노출·tea 미노출 검증 | 2026-08-19 |

**판정: DoD 5/5 통과 — M4 완료 (2026-08-19).** 산출물: bench/tasks.json(선행 12티켓 원문), run3.js(3군 러너 — 선행 레시피 승계·스코어링 교체), parse-handles.js(핸들 12종), aggregate.js(mean±sd·C−B 델타·스키마 게이트). 픽스처는 선행과 동일 커밋 cd83fc1 고정. 파일럿 단가 $0.34·40초/트라이얼 — M5 전체(288트라이얼) 추정 $100~150.

## 롤백
bench/ 이하 독립 — 삭제로 복원. 스킬 산출물에 영향 없음.

## 리스크
- 핸들 추출 전부 0 → 파서가 실제 로그 포맷을 못 읽는 것(RUNBOOK #8). 실제 로그 1건으로 포맷 재확인
- 완료 통지 유실 → 유실 실행 폐기·재실행(RUNBOOK #9)
- 태스크에 계층 C 여지 없음 → 델타가 sd에 묻힘(RUNBOOK #13). 태스크 난이도 사전 점검

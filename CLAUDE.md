# TEA — Token Engineering for Agents (리포 루트)

에이전트 계약은 `harness/CLAUDE.md`가 최우선. 세션 시작: `harness/PROGRESS.md` → 현재 phase(`harness/phases/`) → 관련 `docs/` 절.

검증: `npm run selftest` · `check:budget`(INV-3) · `check:terms`(INV-5) · `check:rules`(INV-6). 구조·경계 규칙은 `docs/FILE_TREE.md`.
